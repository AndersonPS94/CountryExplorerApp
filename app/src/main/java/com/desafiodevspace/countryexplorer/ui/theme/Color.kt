package com.desafiodevspace.countryexplorer.ui.theme

import androidx.compose.ui.graphics.Color

// Cor Primária (Seed Color: Ex: #00897B - Teal 700)
val Primary = Color(0xFF006A61)       // Tonalidade principal (ex: botões de filtro)
val OnPrimary = Color(0xFFFFFFFF)     // Cor do texto sobre a Primária
val PrimaryContainer = Color(0xFF4EEFDE) // Fundo dos chips/botões de filtro ativos
val OnPrimaryContainer = Color(0xFF00201D) // Cor do texto sobre o PrimaryContainer

// Cor Secundária (Geralmente usada para acentuação de elementos secundários)
val Secondary = Color(0xFF4A635F)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFFCEF6E4)
val OnSecondaryContainer = Color(0xFF071F1C)

// Cor Neutra (Usada para Superfícies, Fundo e textos de baixo contraste)
val Surface = Color(0xFFFFFFFF)       // Cor principal do fundo da tela
val Background = Color(0xFFFFFFFF)    // Cor de fundo
val OnSurface = Color(0xFF191C1B)     // Cor para textos principais (sobre a Surface)
val OnBackground = Color(0xFF191C1B)  // Cor para textos principais (sobre o Background)
val SurfaceVariant = Color(0xFFC2CBC9) // Variação de superfície (ex: busca/card)
val OnSurfaceVariant = Color(0xFF3F4947) // Cor para textos/ícones secundários (descrições/inativos)

// Cor de Contorno/Divisores
val Outline = Color(0xFF6F7977)        // Cor de contorno (ex: separadores, campo de busca)

val FavoriteYellow = Color(0xFFFFC107)